import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Navigation } from "@/components/navigation"
import { Footer } from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Do'r Stack Limited - Global Consulting & Contracting Excellence",
  description:
    "Delivering Excellence in Consulting and Contracting for Global Leaders. Professional services in Business Strategy, IT Consulting, Project Management, and Real Estate.",
  keywords: "consulting, contracting, real estate, business strategy, project management, global services",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navigation />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
